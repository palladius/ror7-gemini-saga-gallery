test:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>

local:
  service: Disk
  root: <%= Rails.root.join("storage") %>


# Remember not to checkin your GCS keyfile to a repository
google:
  service: GCS
  project: ror-goldie # TODO(ricc): asbtract from this..
  credentials: <%= Rails.root.join("private/ror-goldie-scooby-n-sagallery.json") %>
  bucket: <%= GcsBucket %> # ricc-public-saga-gallery-<%= Rails.env %>
#  cache_control: "public, max-age=3600"

# Use bin/rails credentials:edit to set the Azure Storage secret (as azure_storage:storage_access_key)

mirror:
  service: Mirror
  primary: local
  mirrors: [ google ]
